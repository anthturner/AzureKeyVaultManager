<Page
    x:Class="AzureKeyVaultManager.UWP.TestPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AzureKeyVaultManager.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="553*"/>
            <RowDefinition Height="36*"/>
            <RowDefinition Height="51*"/>
        </Grid.RowDefinitions>
        <Button x:Name="button" Content="Button" Grid.Row="2" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Click="button_Click"/>
        <TextBox x:Name="textBox" TextWrapping="Wrap" Text="Token" VerticalAlignment="Stretch" Grid.Row="1"/>
        <TextBlock x:Name="textBlock" HorizontalAlignment="Stretch" TextWrapping="Wrap" Text="TextBlock" VerticalAlignment="Stretch"/>

    </Grid>
</Page>
