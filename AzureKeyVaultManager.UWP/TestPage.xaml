<Page
    x:Class="AzureKeyVaultManager.UWP.TestPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AzureKeyVaultManager.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Margin="0,25,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="553*"/>
            <RowDefinition Height="36*"/>
            <RowDefinition Height="51*"/>
        </Grid.RowDefinitions>
        <ListBox x:Name="Subscriptions" >
            <ListBox.Resources>
                <DataTemplate x:Key="SimpleTemplate">
                    <Grid>
                        <TextBlock Text="{Binding SubscriptionId}" />
                    </Grid>
                </DataTemplate>
            </ListBox.Resources>
            <ListBox.ItemTemplate>
                <StaticResource ResourceKey="SimpleTemplate"/>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button Grid.Row="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Content="Click Me" Click="Button_Click_1" />
    </Grid>
</Page>
