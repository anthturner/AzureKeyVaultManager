<UserControl
    x:Class="AzureKeyVaultManager.UWP.ViewControls.KeySecretList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AzureKeyVaultManager.UWP.ViewControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:uwp="using:AzureKeyVaultManager.UWP"
    xmlns:contracts="using:AzureKeyVaultManager.Contracts"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="250">
    <StackPanel Orientation="Vertical">
        <StackPanel Orientation="Horizontal">
            <TextBlock Margin="5 2" Style="{StaticResource SubheaderTextBlockStyle}">Keys &amp; Secrets</TextBlock>
        </StackPanel>
        <Line Height="2" Stroke="{StaticResource ListBoxForegroundThemeBrush}"></Line>


        <ItemsControl ItemsSource="{Binding KeysSecretsSource}">
            <ItemsPanelTemplate>
                <WrapGrid />
            </ItemsPanelTemplate>
            <ItemsControl.ItemTemplateSelector>
                <uwp:CustomDataTemplateSelector />
            </ItemsControl.ItemTemplateSelector>
        </ItemsControl>
    </StackPanel>
</UserControl>
